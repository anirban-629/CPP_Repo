#include <iostream>

// -> import iostream and make it usable in the main function (Header file).
// ? iostream -> input output stream [sequence of bytes]

using namespace std;
int main()
{
    // std::cout << "Hello, world!";
    cout << "Hello, world!";
    // ! <<  <- insertion operator
    // ! >>  <- extraction operator
    return 0;
}
