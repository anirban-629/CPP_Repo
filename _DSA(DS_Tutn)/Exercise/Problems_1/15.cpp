#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    // int a, b, c, s1, s2, s3;
    // cout << "Enter Side 1 -> ";
    // cin >> s1;
    // cout << "Enter Side 2 -> ";
    // cin >> s2;
    // cout << "Enter Side 3 -> ";
    // cin >> s3;
    cout << acos(0.25);
    // a = 1 / acos((pow(b, 2) + pow(c, 2) - pow(a, 2)) / (2 * b * c));
    // b = 1 / acos((pow(a, 2) + pow(c, 2) - pow(b, 2)) / (2 * a * c));
    // c = 1 / acos((pow(a, 2) + pow(b, 2) - pow(c, 2)) / (2 * a * b));

    // cout << a << " " << b << " " << c;

    // ((a + b + c) == 180) ? cout << "TRIANGLE" : cout << "NOT TRIANGLE";
    return 0;
}